# Supabase setup

1. Create a new Supabase project.
2. Open SQL Editor.
3. Run [`schema.sql`](./schema.sql).
4. Run [`seed.sql`](./seed.sql).
5. Copy the project URL and secret key into `.env.local`.
